# 桌面端界面基线

本文是 `packages/client/src/**` 共享客户端和 `apps/desktop/src/renderer/**` 桌面入口当前视觉设计与验收要求的权威文档。它记录已经通过真实页面对照和渲染层实现确认的界面事实，不定义智能体、进程通信、会话或媒体处理行为。

```text
English term                中文术语          介绍
Frontend UI Baseline        前端界面基线      记录桌面端界面当前必须保持的视觉事实。
Conversation                对话工作区        承载用户任务、活动轨迹和智能体回复。
Composer                    输入区            承载输入附件、任务草稿和发送动作。
Attachment                  附件              表示当前选择或已经提交的输入文件，可在同一任务中包含多个；角色只有主视频和音轨两种。
Agent Activity              活动轨迹          按真实执行顺序展示模型工作、工具调用、文件和状态。
Artifact                    产物区            展示智能体生成的输出视频和打开动作。
Visual Verification         视觉验收          通过真实渲染检查空间、层级和状态表现。
Renderer                    渲染层            承载 React 界面和用户交互。
Agent                       智能体            理解任务并调用视频工具完成工作。
Agent Core                  智能体核心        提供与用户界面无关的智能体能力。
Execution Trace             执行追踪          提供运行诊断元数据，主进程据此投影活动轨迹。
```

## 目标与范围

当前界面服务多视频、本机会话持久化、运行时设置、会话重命名/删除与各会话多轮连续处理的视频剪辑闭环。设计目标是安静、紧凑、可扫描的智能体工作区，让主要注意力落在任务、执行过程和产物上，而不是品牌栏或装饰。

当前实现本机会话持久化与重启恢复，并支持会话重命名、删除和最小 Runtime Settings（运行时设置）；不实现会话搜索、视频预览、工作区切换、运行模式选择、预览版标签、任务队列、时间轴、拖拽、新附件类型或通用界面组件库。侧栏展示当前 Agent Desktop、会话列表、“新会话”动作、会话 More 菜单和“设置”。空状态不得演变成欢迎营销页，也不得复制参考产品的品牌与产品概念。

## 当前实现事实

以下内容以当前 Renderer（渲染层）和 Electron Main（主进程）代码为准，是此前视觉对照和交互讨论已经确认并落地的结果。

```text
Area                  中文区域          当前实现
Shell                 页面外壳          桌面宽度为 280px 侧栏加主工作区；820px 以下收缩为 72px 窄侧栏，不隐藏上下文。
Sidebar               工作区侧栏        展示品牌、新会话、运行期会话列表和设置；当前视图明确高亮。
Settings              设置页            编辑当前运行时字段；接口密钥只显示固定掩码、脱敏状态和来源，视频处理只读显示通过系统环境变量查找。
Header                顶部栏            当前不显示独立顶部栏；工作区上下文由侧栏承担。
Conversation          对话工作区        中央阅读轴按轮保留用户任务、活动轨迹、Agent 回复、失败和产物。
Composer              输入区            空状态居中；产生对话后同一输入区停靠底部，不重建输入节点。
Attachment            附件              可选；有一个或多个输入附件时显示 DeepSeek Harness（参考客户端，DSH）文件卡片轨道，没有附件时不渲染空轨道。
Working Directory     工作目录          会话已确认目录时，在输入框上方显示一行只读的“工作目录 + 真实路径”；未确认时不渲染这一行。
Approval              操作审批          宿主有待决操作时，在当前对话末尾显示一张审批卡，展示确切目录并提供“允许 / 拒绝”。
Send / Stop           发送 / 停止       空文本时发送禁用；处理中同一按钮变为可用的停止操作。
Development           开发模式          desktop:dev 通过 Vite Dev Server 提供 CSS、React 和 HMR 热更新。
Production            生产模式          desktop 先构建再启动本地文件，不依赖开发服务器。
```

### 页面骨架

- 桌面布局采用“侧栏 + 主工作区”两栏关系。侧栏是上下文锚点，主工作区承担对话和任务执行。
- 主工作区内部是可滚动对话区和 Composer；不保留独立顶部栏或顶部预留空间。
- 活跃对话只滚动历史内容，输入区占据独立布局空间，不覆盖消息；空状态内容超出窗口高度时从顶部开始滚动，欢迎内容和发送操作都可以到达。
- 默认窗口和窄窗口都保持同一层级；窄窗口只收缩侧栏图标，不把侧栏整体删除，避免顶栏变成孤立卡片。
- 新消息、当前轮活动轨迹更新和 Agent 完成时，对话区只在用户仍停留在最新内容处时跟随滚动；用户向上阅读时保留其位置，主动发送任务、切换会话或打开设置后重新回到最新内容。不引入额外滚动控件或复杂的手动上滚状态。

### 顶部栏

- 当前不显示独立顶部栏；页面顶部不保留空白占位。
- 会话入口和设置统一由侧栏承担。

### Composer 与发送契约

- 发送按钮使用 34px 圆形蓝色主操作，箭头是 18px SVG 图形，不使用 Unicode 字符。
- 输入框为空时，发送按钮必须保留 `disabled`；处理中同一按钮变为“停止”并保持可用。
- 禁用发送按钮使用默认指针，不显示 `not-allowed`，不制造“可执行但失败”的交互暗示。
- 只要有任务文本，即使没有选择主视频也可以发送；这时用户消息不显示附件，主进程向 Agent 发送纯文本任务提示（只有音轨时明确说明它不是可剪辑的主视频）。
- 只有附件没有任务文本时仍不可发送，因为当前 Desktop IPC（桌面进程通信）任务契约要求非空文本。
- “就绪”状态不常驻显示；处理中只在发送按钮旁显示“处理中”。
- 附件 `+` 按钮保留键盘可达性，但不显示额外蓝色焦点外圈。

### 无主视频任务边界

- Renderer 允许无主视频提交有文字的任务。
- Main 不再把选择视频作为所有任务的前置条件；无主视频时不虚构输入视频和输出视频路径。
- `buildAgentPrompt` 在无主视频时只补充事实：本次没有选择输入附件（只有音轨时改为「没有选择主视频，只有音轨文件」并声明音轨不是可剪辑的主输入），以及本次没有预设的最终输出路径。它不追加「一律禁止媒体工具」这类策略——用户可以在任务文字里直接给出路径，由模型对该路径调用已注册工具。
- `createVideoAgent` 在没有语音模型路径时不注册 `transcribe_audio`；选择主视频后仍按现有视频任务配置要求语音模型。
- 有主视频的剪辑任务仍沿用原有输出路径、工具追踪和产物展示链路；输出命名只跟随主视频。

### 工作目录

- 会话已经确认工作目录时，在输入框上方显示一行只读信息：次要色“工作目录”标签加真实路径；路径过长时单行省略，完整路径通过标题提供。未确认目录时不渲染这一行，也不显示占位。
- 工作目录是任务输入上下文的一部分，因此放在输入区而不是侧栏或对话正文；它不参与发送状态，空文本禁用规则不变。
- 批准或拒绝由审批卡承担；这一行只展示结果，不提供修改或清除入口。

### 操作审批

- 宿主有待决目录访问时，在当前对话末尾显示一张审批卡：一行提问“允许 Agent 使用这个目录吗？”，下面用等宽字体显示宿主给出的确切路径，再下面是“允许”和“拒绝”两个动作。
- 审批卡只展示宿主给出的操作，不提供编辑路径或改用其他目录的入口：批准的范围必须和真正要执行的操作完全一致。
- “允许”是主操作，“拒绝”是次要操作；两者都是普通按钮，不引入禁用态或倒计时。
- 用户尚未答复时，工作目录这一行不出现——目录在获批之前不算已经可用。
- 轮次结束（完成、失败或取消）后审批卡立即消失，不能让用户去回答一个已经失效的请求。
- 审批卡属于当前轮，不进入历史：重新加载会话时不会重现旧的审批请求。
- 窄窗口下两个动作允许换行，但不能遮挡目录文本或撑破阅读轴。

## 强制规则与不能做

### 强制规则

- 视觉修改必须保持侧栏、中央对话轴和底部 Composer 的空间关系。
- 空状态和活跃对话必须使用同一个 Composer；切换时不能丢失草稿或重建输入节点。
- 所有发送状态必须遵守“空文本禁用、处理中变停止、有文字无视频可发送”。
- 视觉参考只用于尺寸、间距、密度和层级对照；实际功能仍以当前代码和视频 Agent MVP 为准。
- 修改 Renderer 后使用 `corepack pnpm desktop:dev` 进行实时视觉验收；修改 Main 或 Preload 后重启开发进程。

### 不能做

- 不能因为参考图增加 DSH 的工作区选择、模型选择、权限选择、轨迹页、Token 统计、日志下载、搜索或视频预览入口。
- 不能恢复独立顶部栏、Logo 或品牌副标题；不加入通知等未经本轮范围确认的控件。
- 不能把省略号或状态图标做成没有真实消费者的按钮。
- 不能把无视频任务重新改回“必须选择视频”；也不能把空文本发送改成看起来可提交。
- 不能用大面积空卡片、渐变、品牌插画或高权重 Header 抢占对话区注意力。
- 不能把用户消息和 Agent 回复都做成对称气泡；Agent 回复保持平面正文，工具过程保持紧凑摘要。
- 不能把审批做成可编辑表单或把工作目录做成可自由切换的输入控件：界面只展示宿主要执行的确切操作。

### 明确禁止项

- 禁止把参考截图中的工作区选择、模式选择、预览版标签、品牌标题、模型选择器、插件入口或其他产品概念加入当前客户端。
- 禁止复制 DeepSeek Harness 的插件、插槽、状态存储、工作区运行时、子智能体运行时、技能运行时或其他架构；这里只吸收可验证的空间关系和交互层级。
- 禁止未经本轮需求确认就新增按钮、菜单、附件类型或运行能力；尚未实现的搜索和预览不显示占位控件。
- 禁止把空状态做成营销页、独立欢迎面板或第二套输入区；Composer 必须是当前唯一的任务输入入口。
- 发送按钮在空文本时保持 `disabled`；任务处理中变为可用的“停止”；没有选择视频但有文字时仍可发送。
- 发送按钮禁用时使用默认指针，不显示 `not-allowed` 禁止符号。
- 禁止给附件 `+` 按钮增加蓝色焦点外圈；它保留键盘可达性，但不使用高权重蓝色装饰圈。

## 参考来源

当前视觉方向来自对 DeepSeek Harness 真实页面及以下源码区域的对照：

```text
Source area                  中文名称          本项目吸收的内容
ui-theme                     界面主题          中性浅色层级和语义状态色。
ui-conversation              对话界面          内容宽度轴、消息节奏和输入区关系。
ui-chat                      聊天呈现          用户气泡与 Agent 正文的层级差异。
ui-tool                      活动界面          单行活动摘要和紧凑执行密度。
ui-deliverables              产物界面          轻量文件结果和明确打开动作。
```

DeepSeek Harness 只是视觉参考，不是运行时依赖。不得复制其 `Cordis`、插件、插槽、状态存储、工作区运行时、子智能体运行时或技能运行时。

## 页面结构

### 空对话

- 尚无用户任务、工具活动、智能体回复或错误时，欢迎内容与现有输入区组成一个整体，位于主工作区视觉中心。
- 空状态只显示“开始一个视频任务”和“选择视频，或者直接告诉 Agent 你想做什么。”，输入区仍是首要操作；不显示示例任务、推荐任务或快捷指令。
- 用户发送首个任务后，空状态整体消失；对话内容进入阅读轴，同一个输入区切换到底部停靠位置，不重建输入节点或丢失草稿。
- 默认窗口、最小窗口和窄窗口下，居中整体允许随可用高度滚动，不能把输入按钮挤出可见区域；窄窗口收缩侧栏而不是直接隐藏工作区上下文。

### 工作区侧栏

- 侧栏固定在左侧，使用比主内容略深的中性背景和细右边界，作为工作区上下文的稳定锚点。
- 品牌行采用纯文字布局，只显示 `Agent Desktop`；不使用 Logo（品牌图形）、副标题或没有真实功能的下拉箭头。
- 品牌行垂直节奏保持紧凑：侧栏顶部留白为 16px，品牌行最小高度为 28px，品牌行下方先显示“新会话”，桌面间隔 30px、窄窗口 12px；新会话下方再显示会话列表。
- 侧栏展示当前产品、新会话、运行期会话列表、会话重命名/删除入口和设置；不显示搜索或工作区名称行。
- 会话按钮显示可区分标题，当前会话使用中性高亮；首次发送后标题改为第一条用户任务的简单截断。
- “新会话”是侧栏中的真实按钮；点击后创建并切换到空会话，旧会话继续保留，执行中禁用，不增加确认弹窗。
- 会话行的 More 菜单只提供重命名和删除；重命名支持 Enter/失焦确认、Escape 取消，删除使用行内二次确认并明确不会删除已生成的视频文件。
- 重命名和删除在 Agent 执行期间禁用；删除最后一个会话后由 Host 创建新 Runtime 会话，Client 直接使用 Host 返回的 ID 建立空会话。
- 会话列表可纵向滚动；执行期间全部会话按钮禁用，当前会话可以显示“进行中”。
- 品牌和新会话保持顶部位置，“设置”是侧栏最底部的唯一操作；只有会话列表分配剩余高度。列表保留可见滚动条及稳定槽位，菜单按钮拥有独立列，整行选中背景不能被滚动条裁切或被悬停态覆盖。
- 滚动条稳定槽位占用的宽度折入侧栏内边距：会话行左右留白一致，菜单按钮及其焦点圈不会因为槽位被推离侧栏边界。
- 菜单按钮图标按 16px 原始尺寸绘制三个 3px 圆点，不做小数缩放；按钮悬停时用半透明叠加显示按钮边界，使它在选中行和悬停行上都可辨认。
- 新建或切换会话后让当前行进入列表可见区域；设置页不同时高亮会话。窄侧栏保留按钮的可访问名称和完整标题提示。
- 会话操作使用原生顶层浮层和锚点定位，靠近窗口边缘时翻转方向；列表滚动不能裁切菜单、重命名输入或删除确认。Escape 或点击外部关闭菜单；重命名仍支持 Enter/失焦保存、Escape 取消。
- 侧栏信息密度高于主对话区，但不使用大面积卡片或高权重品牌装饰；窄窗口隐藏标题文字，以圆点区分可点击的会话按钮。

### 设置页

- 设置页导航、分区标题和字段说明统一使用中文，不显示英文技术名。
- 设置页只包含对话模型、视觉模型、语音识别和视频处理当前字段，不增加服务商选择器、检测按钮或路径浏览器。
- 接口密钥不回显已保存或环境变量中的真实值；已配置时在输入框中使用固定八位黑色 `********` 掩码，旁边显示“已配置 / 未配置”和“本机设置 / 环境变量”来源。输入框聚焦后进入新密钥输入状态，未输入内容直接离开时恢复掩码，保存后恢复固定掩码。
- 本机保存的接口密钥可以清除；环境变量来源只读，不能从界面修改或清除。
- Agent 执行期间全部设置输入和保存、清除动作禁用；保存成功明确说明下一次任务生效。
- 设置使用原生模态面板，最大宽 800px、高度不超过窗口减 48px，32px 圆角与浅遮罩。左侧 188px 导航滚动定位四个现有设置分区，右侧内容独立滚动；关闭按钮和 Escape 关闭后焦点返回设置入口，原对话输入节点及草稿保持。560px 以下面板留边 12px、导航收至 92px，字段改为上下排列。加载失败显示错误，不继续显示加载提示。

### 宽度轴与留白

- 正文宽度以主内容列的 64% 自适应，限制在 680–920px；可用宽度不足时按容器收缩，保持约 32px 侧边留白。
- 输入框比正文宽 32px，默认正文 680px、输入框 712px；两者保持共享中心轴。
- 输入框左右留边 16px，正文左右留边约 32px；窄窗口同样保持这一关系。
- 普通消息、活动区和 Agent 回复之间使用 16px 主节奏；活动摘要与正文间为 8px。不得用 28px 以上的大间距把一次任务拆成互不相关的文档章节。
- 空白用于保证阅读和扫描，不使用固定大高度、空卡片或装饰块填充工作区。

## 对话层级

### 用户任务

- 用户任务右对齐，不显示“你”标题和圆形头像。
- 任务正文使用参考浅蓝背景的气泡，圆角 22px，内边距为上下 10px、左右 16px。
- 气泡宽度上限为自适应正文宽度的 70.2%，同时不超过当前可用宽度的 82%；560px 以下可放宽到 86%。
- 已提交附件位于气泡上方并右对齐，使“输入文件”和“任务文字”成为同一用户动作。

### 智能体回复

- 智能体回复保持平面正文，不使用对称聊天气泡。
- 成功回复不显示角色标记和角色文字，直接从正文开始；16px 轻量图形加 12px 次要文字的标记只用于“已停止”和“任务失败”终态，它只说明状态，不充当头像。
- 正文使用 14px 字号和 24px 行高，沿用共享正文宽度轴。
- 正文按最小 Markdown（轻量标记文本）子集渲染：段落、无序/有序列表、围栏代码块、行内代码、加粗和链接；未支持的形式（标题、表格、引用、图片）保持原文，不生成额外结构。用户任务气泡始终按纯文本展示。
- 行内代码用于真实路径、工具名和短命令，长路径在正文宽度内断行；围栏代码块在自己的区域内横向滚动，超长命令不得撑宽阅读轴或页面。
- 链接只在明确的 web 协议上渲染为可点击锚点；桌面宿主把外部地址交给系统浏览器，窗口本身既不导航也不新开窗口。
- 处理期间实时文本沿用同一正文渲染，并在末尾显示表示仍在输出的光标；完成、取消或失败后光标不再显示。
- 处理态、失败态和成功回复沿用同一正文轴；失败使用错误语义色，不增加大型警告卡片。
- 处理期间收到实时文本增量时，在同一正文轴直接展示该临时文本；增量到达前保留紧凑的处理指示，完成、取消或失败后临时文本不再显示，只保留最终回复或终态说明。
- 每轮 Agent 回复拥有自己的活动轨迹和产物；完成新一轮后不得覆盖或混入前一轮内容。
- 界面不显示执行追踪编号；该编号仍由主进程生成并写入本地追踪日志，任务结果契约保持不变。

## 输入区

- 输入区使用 22px 圆角、轻描边和柔和阴影，与页面保持同色表面。
- 空会话文本区最低 52px，活跃会话最低 36px，行高 24px；文本与工具栏之间留 12px。随内容自动增长，最大 128px；不暴露手动缩放手柄。Enter 发送，Shift+Enter 换行，中文输入法组合确认不触发发送。
- 添加附件按钮固定为 28px 圆形，内部使用居中的 14px SVG 加号；悬停时圆形背景明确加深。发送按钮固定为 34px 圆形，二者分别位于左下和右下。
- 发送按钮是唯一高权重操作，使用蓝色主操作色；不可发送时降低透明度，但尺寸和位置不变。
- 空文本时发送禁用；处理中按钮切换为方形停止图形并保持可用；没有视频但有文字时按钮可用，提交的用户消息不显示空附件标签。
- 常态不显示“就绪”。处理任务时可以在发送按钮旁显示低权重的“处理中”。
- 任务成功后清空草稿但保留当前视频集合，任务失败后保留草稿，方便修改或重试。

## 附件

- 附件对齐 DeepSeek Harness（参考客户端，DSH）的 240×64px 文件卡，0.5px 边界、16px 圆角；一次任务可分一次或多次选择多个输入文件，后续选择追加到现有集合。
- 图形容器占 28px，文件名使用 14px 中等字重和 22px 行高，下一行使用 12px 弱化文字显示角色（“视频”或“音轨”）。
- 主视频使用播放三角、音轨使用波形描边：两类输入必须在卡片上可区分，不能只靠文字标签。音轨波形为描边绘制，需显式声明 `stroke` 才能与三角同样清晰。
- 长文件名保持单行并显示省略号，不能扩张卡片或遮挡取消按钮；完整文件名通过卡片标题提供。
- 多个附件以 8px 间距横向并排，超出输入区后横向滚动，不换行增加 Composer 高度。
- 附件轨道保留可见的细滚动条；可移除附件始终为取消按钮预留空间，悬停和键盘聚焦不挤动文件名，触摸设备常显时也不遮挡名称。
- Composer（输入区）中待提交附件的右上角提供 18px 圆形取消按钮；桌面端悬停或键盘聚焦时显示，触摸设备常显。点击后必须同步移除 Renderer（渲染层）和 Electron Main（主进程）中的对应附件。
- 已提交附件使用相同文件卡片结构，但不提供取消或预览操作。
- `+` 附件按钮不显示蓝色焦点外圈；键盘焦点仍可到达按钮，但视觉权重保持为轻量圆形控件。

## 活动轨迹

- 活动区标题行最小高度为 33px，带底部细分隔和矢量展开箭头；活动行最小高度为 28px，13px 动作文字。
- 活动行按真实执行顺序排列：模型步骤行显示“分析任务”和该步真实产生的工具调用数量（0 表示该步直接生成回复）；工具行显示中文动作名和技术工具名。
- 每行依次显示状态符号、动作名、技术工具名或分析结果、真实文件引用、执行状态和耗时。
- 文件引用只出现在宿主确认过的真实路径上：输入文件显示文件名，输入目录和输出目录以 `/` 结尾，输出文件带 → 前缀；点击交给宿主定位，定位失败时在该行内显示真实错误文字，不弹窗、不静默失败。
- 状态只使用执行中、已完成、失败、已停止四种真实状态；耗时来自真实执行时间，超过 1 秒改用秒或分秒展示。
- 中文动作名与技术工具名在空间允许时保持同一行；状态和耗时靠右，便于纵向扫描。
- 行本身不使用卡片边框，只在悬停时显示轻背景；相邻活动行之间用细连接线表达顺序。
- 处理中保持展开；完成后默认折叠为活动数量摘要，并允许用户重新展开。
- 每组活动只显示在产生这些事件的当前 Agent 回复中，多轮之间不得合并活动列表。
- 界面不得出现未由真实事件、参数或结果产生的进度、命令、文件操作或测试数量。
- 5 至 10 项活动同时展示时仍应在默认窗口中保持紧凑，不得让单行信息变成多层面板。

## 产物区

- 产物是智能体回复的一部分，使用参考的浅底文件卡：最小高度 60px、18px 圆角和轻边界；整张卡片不伪装成已可预览的点击区域。
- 视频图形容器固定为 40px；文件名省略显示并提供完整标题提示，下一行显示“视频 · 已完成”。
- “打开文件”是文字型次要操作，保持可见但不与发送按钮竞争视觉权重。
- 多轮产生的产物分别留在对应 Agent 回复中；同一轮真实成功生成多个持久文件时，每个文件各有一张卡片。“打开文件”按该卡片记录的真实路径定位文件，不按文件名猜测，也不始终打开最新产物。
- 文件已被移动或删除时，“打开文件”展示宿主返回的真实错误，不静默失败。
- 窄窗口允许打开动作换到文件信息下方，但不得与文件名或图形重叠。

## 主题与图形

- 页面使用白色基础背景、中性灰文字与边界、蓝色主操作、绿色成功状态和红色失败状态。
- 颜色的具体实现由 `packages/client/src/styles.css` 中当前使用的语义变量负责；本文不复制 HEX 值。
- 不使用大面积单色渐变、装饰光斑、嵌套卡片或品牌插画。
- 当前少量图形不构成引入组件库的理由。若未来真实界面需要统一图标集合，必须先说明消费者与依赖价值，并遵守工程基线。

## 状态验收矩阵

每次影响布局、间距、排版或组件结构时，按本次修改的风险覆盖以下真实状态：

```text
State                        中文状态          验收重点
Empty                        空状态            欢迎内容和输入区整体居中，页面只有一个可用输入区。
Active conversation          活跃对话          空状态消失，同一输入区回到底部，对话进入阅读轴。
Multi-turn conversation      多轮对话          各轮用户消息、Agent 回复、活动和产物保留且归属清楚，最新内容可见。
New session                  新会话            创建并切换到新的空会话，旧会话及其消息、草稿、附件、活动和产物保留在列表中。
Session switch               会话切换          恢复目标会话的消息、草稿、附件、活动和产物，并滚动到最新消息。
Attachment selected          已选择附件        240×64px 文件卡片横向排列，多次选择持续追加且取消行为真实有效。
Working directory set        工作目录已确认    输入框上方显示只读的工作目录行，路径过长时省略且标题给出完整值；未确认时不显示该行。
Approval pending             待决审批          当前对话末尾出现审批卡并展示确切目录；批准前不提前显示工作目录行。
Approval decided             审批已答复        审批卡在答复后消失；目录由宿主确认后才显示工作目录行。
Processing                   处理中            发送变停止；草稿、新会话、会话操作和设置禁用，布局尺寸保持稳定；实时文本替换处理指示。
Cancelled                    已停止            当前回复显示已停止，已完成的活动与产物保留，输入区恢复可继续发送。
Settings                     设置页            字段完整，密钥显示固定星号掩码且来源清楚，视频处理程序只读；保存与清除在处理中禁用。
Failure                      失败              错误可读，草稿保留，不出现大型警告面板。
Completed collapsed          完成并折叠        用户任务、活动摘要、回复和产物层级连续。
Completed expanded           完成并展开        5 至 10 项活动仍紧凑，状态与耗时可扫描。
Long reply                   长回复            列表、代码块、链接和超长路径留在阅读轴内，代码块自身横向滚动。
Scrolled up                  上滚阅读          输出继续时视图不被拉回，回到最新内容后重新跟随。
Activity file reveal failure 定位失败          活动行内显示真实错误文字，不影响其他行和输入区。
Narrow viewport              窄窗口            文件名、活动状态、产物动作和按钮互不遮挡。
Long session list            大量会话          列表独立滚动，底部操作稳定，末尾菜单和删除确认可点击，长短标题不挤压操作，行右边缘不被滚动条槽位推离侧栏边界。
```

桌面主验收尺寸使用接近默认窗口的 1100×750；同时确认应用默认 1120×760、1280×720、800×600 和最小 760×600 下没有结构性问题。涉及 560px 响应式规则时，再补充窄窗口检查。

## 验证流程

自动验证使用项目统一入口：

```bash
corepack pnpm check
corepack pnpm desktop:build
```

视觉验证必须启动正确的应用：

```bash
corepack pnpm desktop:dev
```

开发验收使用 `desktop:dev`，这样 CSS 和 React 修改会即时反映到窗口；生产构建验收使用 `pnpm desktop`。不得直接启动裸 `electron.exe`。确认窗口标题、侧栏、滚动区域和输入区均属于 Agent Desktop，而不是 Electron 默认欢迎页。

测试只验证用户可观察行为，不锁定颜色 HEX、CSS class、像素截图或具体 DOM 层级。视觉验收通过必须来自真实渲染观察，不能由测试或构建通过代替。

## 维护规则

- JSX 和 CSS 是当前实现；本文记录需要跨迭代保持的视觉意图、关键关系和验收标准。
- 有意改变上述基线时，在同一功能分支同步更新本文，并说明真实需求或验收证据。
- 只调整实现细节且不改变基线时，不修改本文。
- `.agents/skills/desktop-frontend/SKILL.md` 只负责执行导航，不重复维护本文件的尺寸与状态清单。
